/* eslint-disable */
/*Generated by GenDocu.com*/
// package: titanium
// file: public/outliers_service.proto

var public_outliers_service_pb = require("../public/outliers_service_pb");
var common_outliers_pb = require("../common/outliers_pb");
var grpc = require("@improbable-eng/grpc-web").grpc;

var OutliersService = (function () {
  function OutliersService() {}
  OutliersService.serviceName = "titanium.OutliersService";
  return OutliersService;
}());

OutliersService.Outliers = {
  methodName: "Outliers",
  service: OutliersService,
  requestStream: false,
  responseStream: false,
  requestType: common_outliers_pb.OutliersRequest,
  responseType: common_outliers_pb.OutliersResponse
};

exports.OutliersService = OutliersService;

function OutliersServiceClient(serviceHost, options) {
  this.serviceHost = serviceHost;
  this.options = options || {};
}

OutliersServiceClient.prototype.outliers = function outliers(requestMessage, metadata, callback) {
  if (arguments.length === 2) {
    callback = arguments[1];
  }
  var client = grpc.unary(OutliersService.Outliers, {
    request: requestMessage,
    host: this.serviceHost,
    metadata: metadata,
    transport: this.options.transport,
    debug: this.options.debug,
    onEnd: function (response) {
      if (callback) {
        if (response.status !== grpc.Code.OK) {
          var err = new Error(response.statusMessage);
          err.code = response.status;
          err.metadata = response.trailers;
          callback(err, null);
        } else {
          callback(null, response.message);
        }
      }
    }
  });
  return {
    cancel: function () {
      callback = null;
      client.close();
    }
  };
};

exports.OutliersServiceClient = OutliersServiceClient;

